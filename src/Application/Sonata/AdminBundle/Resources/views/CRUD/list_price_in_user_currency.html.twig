{% extends admin.getTemplate('base_list_field') %}

{% block field %}
    {% if value is not null %}
        <b>{{ app.user.currency.sign }}</b>&nbsp;{{ app.user.currency.convert(object.price) }}
    {% endif %}
{% endblock %}
